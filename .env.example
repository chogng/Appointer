# Frontend (Vite)
# Use these to point the UI to a different backend.
VITE_API_BASE_URL=/api
# Empty means same-origin (recommended; works with Vite proxy + backend-served dist)
VITE_WS_URL=

# Optional: enable mock frontend (bypasses backend)
VITE_MOCK_API=false
